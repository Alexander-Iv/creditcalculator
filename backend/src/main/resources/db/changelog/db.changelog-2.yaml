databaseChangeLog:
  - changeSet:
      id: 4
      author: alexander.an.ivanov@gmail.com
      runOnChange: true
      changes:
        - sql:
            splitStatements: false
            sql: "DO $$
                   DECLARE
                       rate float;
                   BEGIN
                       for rate in
                           (select generate_series(129,239)/10::float)
                       LOOP
                           RAISE NOTICE 'rate = %', rate;
                           insert into interest_rate(itrt_rate) values (rate);
                       END LOOP;
                   END
               $$"